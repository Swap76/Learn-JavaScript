/*
IIFE: Immediately Invoked Function Expression or Anonymous functions

Functions that are invoked immediately after definition.

Can be used as shown below:--
*/

(function (value) {
  const modified = value + 4;
  console.log(modified);
}(3));